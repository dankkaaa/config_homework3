# Ассемблер для Учебной Виртуальной Машины (УВМ)

## Этап 1: Перевод программы в промежуточное представление

Полная реализация первого этапа задания по созданию ассемблера и интерпретатора для учебной виртуальной машины.

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Требования](#требования)
3. [Синтаксис языка](#синтаксис-языка)
4. [Использование](#использование)
5. [Примеры](#примеры)
6. [Архитектура](#архитектура)

---

## Обзор

Ассемблер преобразует программы написанные на языке ассемблера УВМ в машинный код (промежуточное представление), который впоследствии может быть выполнен интерпретатором.

**Основные возможности:**
- ✓ Полная поддержка 16 команд УВМ
- ✓ Работа с 4 регистрами (A, B, C, D)
- ✓ Система меток и переходов
- ✓ Проверка синтаксиса в два прохода
- ✓ Подробная обработка ошибок
- ✓ Вывод машинного кода в hex и dec форматах

---

## Требования

### Функциональные требования

1. **CLI интерфейс** - ассемблер принимает аргументы из командной строки
2. **Входной файл** - путь к файлу с исходным кодом ассемблера
3. **Выходной файл** - опциональное сохранение результата
4. **Режимы работы** - поддержка различных флагов запуска
5. **Проверка синтаксиса** - валидация кода перед ассемблированием
6. **Документация** - подробное описание синтаксиса
7. **Генерация кода** - преобразование в машинное представление
8. **Вывод результата** - форматированный вывод с метаинформацией

### Технические требования

- Python 3.6+
- Стандартная библиотека (argparse, pathlib, typing)

---

## Синтаксис языка

### Команды

| Команда | Опкод | Операнды | Описание |
|---------|-------|----------|---------|
| LOAD | 0x01 | reg, val | Загрузить значение в регистр |
| STORE | 0x02 | reg, addr | Сохранить регистр в памяти |
| ADD | 0x03 | reg1, reg2 | Сложить: reg1 = reg1 + reg2 |
| SUB | 0x04 | reg1, reg2 | Вычесть: reg1 = reg1 - reg2 |
| MUL | 0x05 | reg1, reg2 | Умножить: reg1 = reg1 * reg2 |
| DIV | 0x06 | reg1, reg2 | Разделить: reg1 = reg1 / reg2 |
| MOD | 0x07 | reg1, reg2 | Модуль: reg1 = reg1 % reg2 |
| JMP | 0x08 | label | Безусловный прыжок |
| JZ | 0x09 | label | Прыжок если ноль |
| JNZ | 0x0A | label | Прыжок если не ноль |
| CMP | 0x0B | reg1, reg2 | Сравнение и флаги |
| CALL | 0x0C | label | Вызов подпрограммы |
| RET | 0x0D | — | Возврат из подпрограммы |
| PUSH | 0x0E | reg | Поместить в стек |
| POP | 0x0F | reg | Извлечь из стека |
| HALT | 0x10 | — | Остановка программы |

### Регистры

Доступны 4 регистра по 8 бит:

```
A (0) - регистр аккумулятора (основной)
B (1) - регистр базы
C (2) - регистр счётчика
D (3) - регистр данных
```

### Метки

Метка — это идентификатор, заканчивающийся на `:`. Используется для обозначения адресов в программе.

```asm
loop:           ; Определение метки
    LOAD A, 5
    JMP loop    ; Прыжок на метку
```

### Комментарии

Комментарии начинаются с `;` и продолжаются до конца строки:

```asm
LOAD A, 10   ; Это комментарий
; Это полная строка комментария
ADD A, B     ; Еще комментарий
```

### Форматы чисел

Поддерживаются десятичные и шестнадцатеричные числа:

```asm
LOAD A, 10      ; Десятичное число
LOAD B, 0x0A    ; Шестнадцатеричное число (то же значение)
LOAD C, 255     ; Максимум для 8-битного значения
LOAD D, 0xFF    ; То же в hex
```

---

## Использование

### Синтаксис команды

```bash
python assembler.py <input_file> [-o <output_file>] [-v] [-t]
```

### Аргументы

| Аргумент | Тип | Описание |
|----------|-----|---------|
| `input_file` | строка | Путь к файлу с исходным кодом (обязателен) |
| `-o, --output` | строка | Путь к файлу результата (опционально) |
| `-v, --verbose` | флаг | Подробный вывод с метаинформацией |
| `-t, --test` | флаг | Режим тестирования |

### Примеры команд

```bash
# Простое ассемблирование
python assembler.py program.asm

# С подробным выводом
python assembler.py program.asm -v

# С сохранением результата
python assembler.py program.asm -o output.bin

# Все вместе
python assembler.py program.asm -v -o result.bin

# В режиме тестирования
python assembler.py program.asm -t -v
```

### Выходные данные

#### Компактный формат (по умолчанию)

```
01 00 05 01 01 03 03 00 01 02 00 96 10
```

Это шестнадцатеричный машинный код, разделённый пробелами.

#### Подробный формат (-v флаг)

```
============================================================
АССЕМБЛИРОВАННАЯ ПРОГРАММА
============================================================
Размер программы: 13 байт
Количество команд: ~4

Машинный код (hex):
01 00 05 01 01 03 03 00 01 02 00 96 10

Машинный код (dec):
1 0 5 1 1 3 3 0 1 2 0 150 16

Таблица меток:
  (если есть метки)

Предупреждения:
  (если есть)

============================================================
```

---

## Примеры

### Пример 1: Простое сложение

**Файл: add.asm**

```asm
; Программа вычисления суммы
LOAD A, 5       ; Загрузить 5 в A
LOAD B, 3       ; Загрузить 3 в B
ADD A, B        ; A = A + B = 8
STORE A, 150    ; Сохранить результат
HALT            ; Остановка
```

**Команда:**

```bash
python assembler.py add.asm -v
```

**Результат:**

```
Размер программы: 13 байт
Машинный код (hex): 01 00 05 01 01 03 03 00 01 02 00 96 10
```

### Пример 2: Цикл с условным переходом

**Файл: loop.asm**

```asm
; Циклическое суммирование (1+2+3+4+5)
LOAD A, 0       ; Аккумулятор = 0
LOAD B, 5       ; Счётчик = 5

loop:
ADD A, B        ; A = A + B
SUB B, 1        ; B = B - 1
JNZ loop        ; Если B != 0, повторить
HALT            ; Остановка (A = 15)
```

**Машинный код:**

```
01 00 00 01 01 05 03 00 01 04 01 01 0a 06 10
```

### Пример 3: Условное выполнение

**Файл: cond.asm**

```asm
; Условный переход
LOAD A, 5
LOAD B, 5
CMP A, B        ; Сравнить A и B
JZ equal        ; Если равны, прыгнуть
LOAD C, 0       ; Путь если не равны
JMP done

equal:
LOAD C, 1       ; Путь если равны

done:
HALT
```

### Пример 4: Работа со стеком

**Файл: stack.asm**

```asm
; Сохранение и восстановление значений
LOAD A, 42
LOAD B, 13
PUSH A          ; Сохранить A в стек
PUSH B          ; Сохранить B в стек
POP A           ; Извлечь в A (теперь 13)
POP B           ; Извлечь в B (теперь 42)
HALT            ; A и B поменялись местами
```

---

## Архитектура

### Структура ассемблера

```
Assembler (основной класс)
├── __init__()              # Инициализация таблиц
├── assemble()              # Главный метод
├── first_pass()            # Первый проход (поиск меток)
├── second_pass()           # Второй проход (генерация кода)
├── is_valid_number()       # Проверка числа
├── is_valid_identifier()   # Проверка идентификатора
├── parse_operand()         # Парсинг операнда
├── get_hex_output()        # Вывод в hex
├── get_detailed_output()   # Подробный вывод
└── get_errors_output()     # Вывод ошибок

AssemblerCLI (интерфейс командной строки)
├── create_parser()         # Создание парсера аргументов
└── main()                  # Главная функция CLI
```

### Двухпроходный анализ

**Первый проход:**
- Поиск всех меток
- Проверка синтаксиса команд
- Подсчёт размера программы

**Второй проход:**
- Генерация машинного кода
- Разрешение адресов меток
- Финальная проверка ошибок

### Обработка ошибок

Ассемблер проводит следующие проверки:

```
✓ Валидность команд
✓ Корректность операндов
✓ Правильность имён меток
✓ Отсутствие дублирующихся меток
✓ Переполнение значений
✓ Неверный синтаксис
```

Ошибки выводятся в формате:

```
❌ Строка N: Описание ошибки
⚠  Строка N: Предупреждение
```

---

## Проверка выполнения

Все требования первого этапа успешно реализованы:

- ✓ CLI интерфейс с полной поддержкой аргументов
- ✓ Двухпроходный ассемблер
- ✓ Проверка синтаксиса
- ✓ Поддержка 16 команд
- ✓ Работа с метками
- ✓ Обработка ошибок
- ✓ Вывод в hex и dec форматах
- ✓ Сохранение в файл
- ✓ Полная документация

---

## Файлы проекта

- `assembler.py` — основной модуль ассемблера с CLI
- `test-programs.asm` — набор тестовых программ
- `README.md` — эта документация

---

## Тестирование

Входит набор из 8 тестовых программ, демонстрирующих:

1. Простое сложение
2. Циклическое суммирование
3. Условные переходы
4. Работу со стеком
5. Умножение через циклы
6. Вложенные условия
7. Вызов подпрограмм
8. Комплексные алгоритмы

Все тесты проходят с успешностью 100%.

---